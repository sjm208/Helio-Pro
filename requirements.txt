# HELIO Pro — Python dependencies
# ─────────────────────────────────────────────────────────────────
# Install all required libraries:
#   pip install -r requirements.txt
#
# New to Python? Read INSTALL.md first.
# ─────────────────────────────────────────────────────────────────

# ── Core UI ──────────────────────────────────────────────────────
customtkinter>=5.2.2
Pillow>=10.0.0

# ── Numerical / image processing ─────────────────────────────────
numpy>=1.24.0
opencv-python>=4.8.0
scipy>=1.11.0
matplotlib>=3.7.0

# ── 3D viewer ────────────────────────────────────────────────────
vispy>=0.14.0
PyOpenGL>=3.1.7

# ── 3D mesh export ───────────────────────────────────────────────
trimesh>=4.0.0

# ── TIFF and advanced image I/O ──────────────────────────────────
tifffile>=2023.1.1

# ── Computer vision extras ───────────────────────────────────────
scikit-image>=0.21.0
scikit-learn>=1.3.0

# ── GIS export — georeferenced TIFFs ─────────────────────────────
# Required for GeoTIFF, hillshade, and QGIS export features
rasterio>=1.3.0

# ── Terrain analysis — RVT algorithms ───────────────────────────
# Required for Sky-View Factor, MSRM, SLRM, ASVF engines
# Zakšek et al. 2011; Kokalj & Hesse 2017 — Apache 2.0
# https://github.com/EarthObservation/RVT_py
rvt-py>=2.2.0

# ── E57 point cloud export ───────────────────────────────────────
# Required for CloudCompare / Leica / FARO export
pye57>=0.4.8

# ── QR code generation ───────────────────────────────────────────
# Required for QR codes in export packages and archive bundles
qrcode[pil]>=7.4.2

# ── Optional: Neural PS solver (~2 GB download) ──────────────────
# Remove the # below to enable PyTorch Deep PS solver:
# torch>=2.1.0
# torchvision>=0.16.0

# ── Optional: Anthropic Claude AI analysis ───────────────────────
# Requires an Anthropic API key — see AI Tools tab
# Remove the # below to enable:
# anthropic>=0.25.0
